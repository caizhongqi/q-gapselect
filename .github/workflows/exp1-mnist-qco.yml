name: exp1-mnist-qco

on:
  push:
    branches:
      - scratch/exp1-mnist-qco
    paths:
      - 'scratch/exp1_mnist_qco.py'
      - '.github/workflows/exp1-mnist-qco.yml'
  workflow_dispatch:

jobs:
  run-exp1:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
      - name: Syntax check
        run: python -m py_compile scratch/exp1_mnist_qco.py
      - name: Run Experiment 1
        run: |
          python scratch/exp1_mnist_qco.py \
            --epochs 3 \
            --targets 20 \
            --pool 512 \
            --out exp1_qco_out
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exp1-mnist-qco-results
          path: exp1_qco_out
          if-no-files-found: warn

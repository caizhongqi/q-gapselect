name: qcollide-neural-atlas-time-ettm1

on:
  workflow_dispatch:
  push:
    branches:
      - agent/qcollide-neural-atlas
    paths:
      - '.github/workflows/qcollide-neural-atlas-time-ettm1.yml'
      - 'configs/qcollide_time_ettm1_phase2.json'
      - 'src/qgapselect/qcollide/time_atlas.py'
      - 'src/qgapselect/qcollide/time_atlas_results.py'
      - 'scripts/run_qcollide_time_atlas.py'
      - 'scripts/merge_qcollide_time_atlas.py'
      - 'tests/test_qcollide_time_atlas.py'
      - 'pyproject.toml'

concurrency:
  group: qcollide-neural-atlas-time-ettm1-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python -m pytest tests/test_qcollide_time_atlas.py -q
      - run: >-
          python -m ruff check
          src/qgapselect/qcollide/time_atlas.py
          src/qgapselect/qcollide/time_atlas_results.py
          scripts/run_qcollide_time_atlas.py
          scripts/merge_qcollide_time_atlas.py
          tests/test_qcollide_time_atlas.py

  component:
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        architecture: [lstm, tcn, timesnet, patchtst]
        seed: [0, 1, 2]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[atlas_time]'
      - uses: actions/cache@v4
        with:
          path: .cache/qcollide-time
          key: qcollide-ettm1-${{ runner.os }}
      - run: mkdir -p artifacts-ci
      - name: Run ETTm1 time-series atlas component
        run: >-
          python scripts/run_qcollide_time_atlas.py
          --config configs/qcollide_time_ettm1_phase2.json
          --architecture ${{ matrix.architecture }}
          --model-seed ${{ matrix.seed }}
          --output artifacts-ci/component.json
      - uses: actions/upload-artifact@v4
        with:
          name: qcollide-time-ettm1-${{ matrix.architecture }}-${{ matrix.seed }}
          path: artifacts-ci/component.json
          if-no-files-found: error
          retention-days: 21

  merge:
    needs: component
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e .
      - uses: actions/download-artifact@v4
        with:
          pattern: qcollide-time-ettm1-*
          path: artifacts-components
          merge-multiple: true
      - run: mkdir -p artifacts-ci
      - run: >-
          python scripts/merge_qcollide_time_atlas.py
          --input-directory artifacts-components
          --config configs/qcollide_time_ettm1_phase2.json
          --output artifacts-ci/qcollide_time_ettm1_phase2_merged.json
      - uses: actions/upload-artifact@v4
        with:
          name: qcollide-time-ettm1-phase2-merged
          path: artifacts-ci/qcollide_time_ettm1_phase2_merged.json
          if-no-files-found: error
          retention-days: 21

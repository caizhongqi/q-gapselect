name: exp4b-strong-refiner-audit

on:
  push:
    branches:
      - scratch/exp1-mnist-qco
    paths:
      - 'scratch/exp4b_strong_refiner_audit.py'
      - '.github/workflows/exp4b-strong-refiner-audit.yml'
  workflow_dispatch:

jobs:
  run-exp4b:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
      - name: Syntax check
        run: python -m py_compile scratch/exp4b_strong_refiner_audit.py
      - name: Run Experiment 4B
        run: |
          python scratch/exp4b_strong_refiner_audit.py \
            --epochs 3 \
            --targets 10 \
            --pool 512 \
            --seed-budget 128 \
            --steps 120 \
            --out exp4b_out
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exp4b-strong-refiner-audit-results
          path: exp4b_out
          if-no-files-found: warn

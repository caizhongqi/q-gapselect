name: qcollide-cifar10-repair-calibrated-v2

on:
  workflow_dispatch:
  push:
    branches:
      - agent/qcollide-neural-atlas
    paths:
      - '.github/workflows/qcollide-cifar10-repair-calibrated-v2.yml'
      - 'configs/qcollide_cifar10_repair_calibrated_v2.json'
      - 'src/qgapselect/qcollide/cifar_repair_calibrated.py'
      - 'src/qgapselect/qcollide/cifar_repair_calibrated_results.py'
      - 'scripts/run_qcollide_cifar_repair_calibrated_v2.py'
      - 'scripts/merge_qcollide_cifar_repair_calibrated_v2.py'
      - 'tests/test_qcollide_cifar_repair_calibrated_v2.py'

concurrency:
  group: qcollide-cifar10-repair-calibrated-v2-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: >-
          python -m pip install --index-url https://download.pytorch.org/whl/cpu
          torch torchvision
      - run: python -m pip install 'scikit-learn>=1.9'
      - run: >-
          python -m pytest
          tests/test_qcollide_cifar_repair_calibrated_v2.py
          -q
      - run: >-
          python -m ruff check
          src/qgapselect/qcollide/cifar_repair_calibrated.py
          src/qgapselect/qcollide/cifar_repair_calibrated_results.py
          scripts/run_qcollide_cifar_repair_calibrated_v2.py
          scripts/merge_qcollide_cifar_repair_calibrated_v2.py
          tests/test_qcollide_cifar_repair_calibrated_v2.py

  prepare-data:
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: >-
          python -m pip install
          'numpy>=1.24'
          'datasets>=2.20,<5'
          'Pillow>=10'
      - uses: actions/cache@v4
        id: raw-cache
        with:
          path: .cache/qcollide-repair-cifar10
          key: qcollide-cifar10-repair-snapshot-v1-${{ runner.os }}
      - name: Freeze CIFAR-10 raw snapshot if cache is absent
        if: steps.raw-cache.outputs.cache-hit != 'true'
        run: >-
          python scripts/prepare_qcollide_cifar10_hf_snapshot.py
          --output-directory .cache/qcollide-repair-cifar10
      - name: Verify prepared snapshot
        run: |
          test -f .cache/qcollide-repair-cifar10/train_images.npy
          test -f .cache/qcollide-repair-cifar10/train_labels.npy
          test -f .cache/qcollide-repair-cifar10/evaluation_images.npy
          test -f .cache/qcollide-repair-cifar10/evaluation_labels.npy
          test -f .cache/qcollide-repair-cifar10/snapshot_metadata.json

  component:
    needs: prepare-data
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        architecture: [resnet18, vit_tiny, mlp_mixer]
        seed: [0, 1, 2]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e .
      - run: >-
          python -m pip install --index-url https://download.pytorch.org/whl/cpu
          torch torchvision
      - run: python -m pip install 'scikit-learn>=1.9'
      - uses: actions/cache@v4
        with:
          path: .cache/qcollide-repair-cifar10
          key: qcollide-cifar10-repair-snapshot-v1-${{ runner.os }}
          fail-on-cache-miss: true
      - run: mkdir -p artifacts-ci
      - name: Run calibrated soft-energy repair component
        run: >-
          python scripts/run_qcollide_cifar_repair_calibrated_v2.py
          --config configs/qcollide_cifar10_repair_calibrated_v2.json
          --architecture ${{ matrix.architecture }}
          --model-seed ${{ matrix.seed }}
          --output artifacts-ci/component.json
      - uses: actions/upload-artifact@v4
        with:
          name: qcollide-cifar10-repair-v2-${{ matrix.architecture }}-${{ matrix.seed }}
          path: artifacts-ci/component.json
          if-no-files-found: error
          retention-days: 21

  merge:
    needs: component
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e .
      - uses: actions/download-artifact@v4
        with:
          pattern: qcollide-cifar10-repair-v2-*
          path: artifacts-components
      - run: mkdir -p artifacts-ci
      - run: >-
          python scripts/merge_qcollide_cifar_repair_calibrated_v2.py
          --input-directory artifacts-components
          --config configs/qcollide_cifar10_repair_calibrated_v2.json
          --output artifacts-ci/qcollide_cifar10_repair_calibrated_v2.json
      - uses: actions/upload-artifact@v4
        with:
          name: qcollide-cifar10-repair-calibrated-v2
          path: artifacts-ci/qcollide_cifar10_repair_calibrated_v2.json
          if-no-files-found: error
          retention-days: 21

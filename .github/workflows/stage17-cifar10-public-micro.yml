name: stage17-cifar10-public-micro

on:
  push:
    branches:
      - scratch/stage9-functional-collision
    paths:
      - scratch/stage17_cifar10_public_micro.py
      - .github/workflows/stage17-cifar10-public-micro.yml
  workflow_dispatch:

jobs:
  micro:
    strategy:
      fail-fast: false
      matrix:
        model: [cifar10_resnet20, cifar10_mobilenetv2_x0_5, cifar10_vgg11_bn]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy torch torchvision --index-url https://download.pytorch.org/whl/cpu
      - name: Syntax check
        run: |
          python -m py_compile scratch/stage17_cifar10_pretrained.py
          python -m py_compile scratch/stage17_cifar10_public_micro.py
      - name: Run public-only micro
        run: |
          cd scratch
          python stage17_cifar10_public_micro.py \
            --model ${{ matrix.model }} \
            --out ../stage17_micro_out
      - name: Upload micro artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage17-micro-${{ matrix.model }}
          path: stage17_micro_out
          if-no-files-found: warn
